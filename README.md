# GitOps Managed Kubernetes Infrastructure

This repo holds the infrastructure as code for my production grade kubenetes homelab, entirely with GitOps and Flux CD.

Currently deployed to a local K3s cluster with a migration to Talos Linux in progress.